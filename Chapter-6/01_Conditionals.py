#Conditional Statements == Simply if-else statements:

"""
For example,
If it's clear weather you go outside.
If it's not clear/ if it's raining you stay inside.

That means : If its clear go outside, else stay inside.

"""
#Simple conditional problem example:

a = int (input("Enter your age: "))

if(a>18):
    print("You are at the legal age now")

else:
    print("You are still a kid")
